# Optional — direct TMDB calls in local dev only
VITE_TMDB_API_KEY=

# Netlify server function (production)
TMDB_API_KEY=

# Optional — shared live feed + real auth (disable email confirm in Supabase for fastest demo)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
